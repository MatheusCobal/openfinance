[project]
name = "openfinance"
version = "0.1.0"
dependencies = [
    "fastapi[standard]",
    "sqlmodel",
    "httpx",
    "pydantic-settings",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
